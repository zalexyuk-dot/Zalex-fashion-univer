# Zalex-fashion-univer
&lt;script> document.querySelector('form').addEventListener('submit', function(e) {     e.preventDefault();     if (this.checkValidity()) {         alert('Message sent! (Demo - integrate backend for real emails)');         this.reset();     } else {         alert('Please fill all fields correctly.');     } }); &lt;/script>
